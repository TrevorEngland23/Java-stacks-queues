public class Node {
    //data - fields
    int value;
    Node next;

    //actions - methods

    //init - ctor(s)
    public Node(int initialValue) {
       value = initialValue;
       //next = null;
    }
}
